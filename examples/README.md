# Examples

Put example **problem** files here (organized by chapter, e.g. `chapter2/`).

⚠️ Do **not** upload solution files while they are still in use. Share solutions
separately (e.g. after the exam) if you choose.
